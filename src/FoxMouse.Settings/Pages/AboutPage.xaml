<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FoxMouse.Settings.Pages.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="Transparent"
    mc:Ignorable="d">

    <ScrollViewer
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto">
        <Grid
            MaxWidth="900"
            Padding="32,24,32,40"
            HorizontalAlignment="Stretch">
            <StackPanel Spacing="12">
                <StackPanel Margin="0,0,0,16" HorizontalAlignment="Center" Spacing="8">
                    <Border
                        Width="72"
                        Height="72"
                        CornerRadius="18">
                        <Image
                            x:Name="BrandLogoImage"
                            AutomationProperties.Name="FoxMouse Logo"
                            Stretch="Uniform" />
                    </Border>
                    <TextBlock
                        HorizontalAlignment="Center"
                        Style="{StaticResource TitleTextBlockStyle}"
                        Text="FoxMouse" />
                </StackPanel>

                <Border Style="{StaticResource FoxMouseCardStyle}">
                    <Grid ColumnSpacing="24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Uid="AboutPageString1" Style="{StaticResource BodyStrongTextBlockStyle}" Text="版本" />
                        <TextBlock Grid.Column="1" Text="{Binding Version}" />
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
