# 在线引导器首屏实机检查（2026-09-14）

本轮直接打开在线引导器，只选择语言和关闭窗口，未点击继续、未下载安装组件。

## 发现与修复

- 原在线候选 `online-candidate-1f6c3190461a4de1956ecac4e6c1678d` 实际首屏裁切中英文说明，底部操作按钮位于可视区域外。此前维护窗口截图不覆盖此入口。
- `BootstrapWindow` 构造过程中未挂起布局，且单列未指定百分比宽度。增加构造期 SuspendLayout/ResumeLayout，显式 100% 列宽，以实际内容宽度约束说明换行。
- 修复版独立引导器：`artifacts/bootstrap-layout-bc263968ac7a43d787fa75408ce712cd/FoxMouse-OnlineSetup-x64.exe`。复用此前候选的 Setup 载荷，不代表全套最终发布包已经更新。

## 直接界面证据

通过 computer-use 实际观察：中文首屏标题、说明、语言框、状态、继续/关闭全部可见；展开语言列表有简体中文与 English，列表暗色底、选中行系统蓝色，未出现白色底块。选择 English 后标题及所有文案切换，说明换为两行，Continue / Close 可见、互不重叠。

点击英文 Close 后，随后进程查询未发现任何 FoxMouse-OnlineSetup-x64.exe，确认退出。没有操作系统主题切换，不能将此次深色结果外推为高对比度或动态主题通过。

## 自动回归

- 新增 BootstrapLayoutTests：中文/英文 × 620/540 客户区宽度，共 4 项。真实构造并显示生产 BootstrapWindow，检查直属控件及按钮边界均在父容器内，且无滚动页。
- Deployment 全套 151/151 通过，0 跳过。其他项目本轮未重复测试；此前整个方案为 504/504，因此不要将本轮写成整个方案已跑 508 项。
- 测试链接生产 BootstrapProgram 源码；现代测试编译条件仅隔离入口名及 .NET Framework 兼容警告，生产仍由 C# 5 编译。

仍须完成干净 Windows 依赖安装链路、其他未决验收及一致产物重建。旧在线候选不能继续作为包含本修复的最终包。

## 后续完整打包回归

首次完整打包在 Windows 自带 C# 5 编译器失败（CS1024）：它即使在不活动的条件编译区内也不识别 `#nullable`。已改成条件内兼容的具体警告抑制，未降低整个项目的警告检查；现代测试入口与生产入口仍分别编译。打包脚本现在会将编译器具体诊断包含在失败异常中，避免被调用方收集输出后丢失。

修复后完整在线候选 `online-candidate-e57fc5880b4246d180d546ad2d1a8016` 构建通过，31,226,880 字节，SHA256 `31F2F2CFF99CB3F6CEBBBF5A2B822FB7EA386766BB410AE19774DFBE0003DAED`。通过静态产物校验、英文 CLI 隔离部署及整包隔离生命周期。失败候选 `online-candidate-adf60e8c4fb645b9a990bf5014556bdf` 未覆盖或当作交付。

本次完整 Debug 测试 508/508 通过；修改兼容指令后受影响的 Deployment 151/151 再次通过。离线/便携最终重建、干净 Windows、实际光标可见性等原有未决项不因这一轮打包通过而视为完成。
