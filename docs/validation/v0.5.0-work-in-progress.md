# FoxMouse v0.5.0 开发与验收进度（未发布）

当前统一验收入口：`v0.5.0-acceptance-summary.md`。本文保留历史过程，后续记录可能替代早期“待完成”或测试数字；不要据本文单一段落认定当前发布完成。

更新：2026-09-14。目标仍在进行中；源码版本已更新为 0.5.0，仍为未发布候选，旧发行包没有替换。下文早期 0.4.2 记录为历史验证。

## 定位环边缘像素回归

- 将实际 ShowLocator 所用圆环绘制提取为共用 DrawLocatorRing；没有另写一套测试专用绘制替代实现。
- 新增 6 组 PArgb 位图像素检查，覆盖 DPI 比例 1/1.5/2/3/4 与不同进度；检查可见边缘保持橙色色相、存在半透明抗锯齿、中心与画布角落透明。白色或灰白色描边会违反色相断言。
- Platform 测试 196/196 通过，0 跳过，退出码 0；证据 `artifacts/platform-tests-latest.log`。检查的是与生产绘制相同的图形函数和像素格式，尚不代表桌面最终合成、物理 8K 或多显示器全部验收。

## 0.5.0 候选统一版本与回归

### 备用界面语言保留修复

- SettingsForm 后续也已完成资源化：字段标题、章节、模式、快捷键按钮、悬停/无障碍说明、数量/上限/灵敏度动态文字全部使用 UiText。SettingsForm 与 ProcessPickerDialog 中硬编码中文扫描均为 0；完整英文排版仍需实机核对。
- 本轮全量 Release 测试 489/489（Core 100、Deployment 140、Settings 59、Platform 190），无跳过，退出码 0；`artifacts/tests-latest.log`。此前“SettingsForm 静态文字待翻译”属于历史状态，现已补齐代码。

- 后续已将 ProcessPickerDialog 的搜索、后台进程、刷新、浏览 EXE、列表列名、无障碍说明、空列表/加载失败状态、数量与重复添加提示全部接入中英文资源；源文件中文残留扫描为 0。Settings 测试 59/59 仍通过。
- 英文备用选择器的真实小窗口/DPI 排版仍未验收；备用 SettingsForm 静态文字仍待处理，不能将源代码无中文等同于全部界面验收。

- 检查发现 SettingsForm.Result 重新创建 FoxMouseSettings，导致未在备用界面暴露的 Language 被重置为 system。改为基于原归一化配置使用 with 更新，保留语言及其他未暴露字段。
- 新增中英文语言保留回归测试；Settings 测试 59/59 通过。
- 备用 AboutForm 的标题、版本、关闭及无障碍文字全部使用 UiText；主程序启动错误提示加入中英文资源。
- 备用 SettingsForm 和 ProcessPickerDialog 其余静态文字仍待翻译，不能据此宣布英文版全部完成。本次改动在上一候选构建之后，需要最终重建。

- Directory.Build.props 统一设为 0.5.0 / 0.5.0.0；新增 New-NativeVersionSource.ps1 为内置 C# 编译的在线引导器和 Cleanup 生成同源版本资源。引导器打包时拒绝混入不同版本 Setup。
- 候选 `online-candidate-bb8c1c2bf5d04012a8a6975ef2c356dd` 构建通过。7 个可执行文件（在线引导器、Setup、App、Guard、Uninstall、Cleanup、Settings）的 FileVersion 全部实测为 0.5.0.0。
- 在线包 31,202,304 字节，SHA256 `96E6D5A13ACCF6BA653C0CED90362075A0CE4E8793F11C7747478774776F31A9`。它仍是未签名、未正式发布的候选。
- 0.5.0 全量测试 487/487 通过；同一候选完整隔离安装维护生命周期再次通过（证据标识 85677724293b4e279ea055d08df344cc）。日志为 `artifacts/tests-latest.log` 与 `artifacts/online-lifecycle-latest.log`。
- CHANGELOG 标为 Unreleased candidate，明确剩余验收未结束，不代表正式交付。

## 微软依赖真实下载验证

- 新增只下载/校验、绝不执行载荷的 `installer/BootstrapDownloadProbe.cs`，使用与在线引导器相同的 BootstrapDownloader/BootstrapDependencyPolicy，在 Windows 自带 .NET Framework 编译并运行。
- 从生产代码固定的微软地址真实下载两项依赖，均退出 0，SHA-512 与引导器固定值完全匹配：.NET Desktop Runtime 10.0.11 x64 为 60,001,888 字节；Windows App Runtime 2.4.0 x64 为 116,423,480 字节。App Runtime 的 aka.ms 重定向链也由实际下载器验证通过。
- 两份下载的 Authenticode 状态均为 Valid；签名者分别为 .NET / Microsoft Corporation 与 Microsoft Corporation。
- 文件保留于 `artifacts/download-verification-fb6c3cc92dd44007ac6bbd3ccd25873f`，未运行依赖安装程序、未改动系统运行库。此项证明真实网络下载及完整性，不代表缺失依赖机器上的安装或重启分支已通过。

## 退出联动分支回归

- 新增 `--interactive-ui-test` 设置入口：独立 AppInstance、进程号+随机临时配置目录、不自动关闭，供真实 UI 操作验收；退出监听绑定其临时 settings.json，不竞争生产退出通道。
- rootDirectory 非空的测试 ViewModel 使用不发送通知的 notifier，保存仍写临时配置，但不通知真实后台应用或触发真实预览。原普通启动和自动 window-smoke 行为保留。
- 修改后 Settings 59/59 通过。隔离入口已准备，尚未据此宣布真实弹窗分支已验收。

- 将 MainWindow 保存弹窗后的关闭判断提取为实际调用的 SettingsClosePolicy，覆盖全局退出/普通关闭下 Save、Discard、Cancel 以及保存失败，共 8 个分支测试。全局 Cancel 退出、普通 Cancel 保留；保存失败始终保留窗口，避免丢失修改。
- 命名管道增加连续两次请求测试，验证第一次退出处理后仍能接收后续请求；原有等待用户决策与配置路径隔离测试仍通过。该测试只证明 IPC 连续请求，不声称已验证真实窗口重开过程。
- 检查 SettingsHostBridge.DisposeAsync 只取消设置变更监听，不终止设置进程；主进程恢复光标后才请求设置关闭。
- 全量 Release 测试 487/487（Core 100、Deployment 140、Settings 57、Platform 190），0 跳过，退出码 0；证据 `artifacts/tests-latest.log`。托盘点击→实际保存弹窗→两个进程退出仍待端到端实机验收。

## 下载取消与异常验收

- 补充 5 个实际流读取测试：请求前取消不访问服务器、首块后取消清零、首块后断流清零、无 Content-Length 分块下载仍限制大小、声明长度不完整即使哈希可匹配也拒绝。Deployment 140/140 通过。
- 代码检查发现引导器一直传 CancellationToken.None，关闭操作无法及时中止下载。改成窗口生命周期取消源，点击关闭时取消下载，取消成功按用户正常关闭处理；已启动的外部组件安装进程仍等待结束，不强杀。
- 同时为在线首屏语言框补上 180 DIP 最小宽度，避免表格布局挤压。Windows 内置 .NET Framework 编译通过，引导器 smoke 退出 0。候选验证文件：`artifacts/online-candidate-d9a3da1a62e8403db36cc81f4a336e03/FoxMouse-OnlineSetup-cancel-test.exe`。
- 这些证据覆盖下载器的离线故障注入与引导器构建/启动，不替代真实缺失依赖环境下的下载取消和组件安装验收。

## 静止恢复验收补齐（未通过）

### 后续成功复测（系统状态层面）

- 2026-09-14 12:35–12:36 UTC，确认没有运行中的 FoxMouse 后，对同一候选完成 1 次与连续 5 次静止测试，共 6/6 退出 0。每次均观察到真实原生替换激活、动画自然结束、图层隐藏/Guard 恢复、坐标全程不变、交接 350 ms 后系统报告可见。
- 每次外层安全恢复均退出 0；测试后没有留下 FoxMouse 主程序。证据已复制到 `artifacts/validation/stationary-recovery-20260914`，避免只依赖临时目录。
- 日志仍有短期 redraw unconfirmed，故不能声称瞬时交接或所有应用下恢复都已证明。已向用户请求肉眼确认；本结果不代表真实像素显示、长期稳定性、高权限窗口和多屏场景全部通过。此前坐标移动无效轮仍保持无效，不纳入 6/6。

- 发现旧 EnginePreviewSmokeRunner 在观察到放大生效后立即 Dispose，实际只覆盖启动/退出恢复，不覆盖自然缩小后的静止交接。新增独立 `--stationary-cursor-smoke --allow-real-cursor-hide`：必须显式允许真实隐藏，获得产品单实例锁后才测试；要求放大真正生效、自然结束且图层与 Guard 都交还原生光标，350 ms 后检查系统可见状态，鼠标坐标变化立即退出 34，保留日志。
- 该检查仍只是系统可见状态证据，不等于最终肉眼像素证明。没有移动/锁定鼠标，也没有替换系统光标主题。
- 用户确认旧版退出后进行了三次实测，均因坐标变化返回 34（最后一次从 191,564 到 194,569），不能计为恢复通过或功能失败。每次 finally 执行 Guard `--restore-cursor` 均退出 0；测试退出完成。
- 新测试代码构建通过，候选 `online-candidate-d9a3da1a62e8403db36cc81f4a336e03`。保留 `%TEMP%/FoxMouse-Engine-Smoke-*` 日志。仍需安静输入环境中完成自然结束静止恢复、重复触发与视觉核对。
- Microsoft 官方光标说明确认 SetCursor 只在调用线程拥有鼠标输入时生效，不能将跨线程调用成功或 GetCursorInfo 标志独自当作渲染已恢复的充分证据：https://github.com/MicrosoftDocs/win32/blob/docs/desktop-src/menurc/about-cursors.md 。

## 安装语言传递补齐

- 后续补齐独立卸载器入口：读取 settings.json 的 language 后初始化维护界面；缺失、损坏、非法值或读取失败回退系统语言，不改写/隔离用户配置。对读取输入加 1 MiB 上限。
- 新增 9 个读取/回退/不改写测试，Deployment 135/135 通过。
- 新候选 `online-candidate-787bcf13053340bcad75206522972f32` 实际隔离安装、英文独立维护窗口、缓存修复和卸载闭环通过。维护窗口证据新增 displayLanguage/title，测试确认 en-US 与 FoxMouse Maintenance，而非仅检查进程退出码；配置修复前后逐字一致。完整中英文视觉矩阵仍待最终验收。

- InstallRequest 增加 InitialLanguage，图形维护窗口与安装 CLI 均传入当前所选语言。仅首次安装且 settings.json 不存在时初始化 language；使用临时文件再不覆盖移动发布，保留重装时留存的原始配置。
- 修复/升级不重写语言；安装失败时回滚本轮新增且未被应用修改的初始化配置。
- 新增中英首次安装/修复保留、重装保留原始设置、失败回滚测试，Deployment 测试 126/126 通过。
- 候选 `online-candidate-896070a2c414453295f6aff18681e875` 实际隔离 CLI 闭环通过：英文安装生成 language=en-US，缓存修复后配置逐字不变，随后卸载通过。运行证据为 `artifacts/online-deployment-cli-latest.log`。仍需对首次运行 UI 及卸载器所用语言作最终实机核对。

## 2026-09-14 完整在线候选包构建

### 安装界面实机核对与生命周期门禁恢复

- computer-use 实际查看候选维护页：窗口确实正常绘制，发现首屏语言框缩成只能显示部分文字。为语言框添加 170 DIP 最小宽度与左对齐，并增加宽度回归断言；Deployment 122 个测试通过。后续 144 DPI 安装页真实 HWND 截图中“简体中文”完整显示。
- 空白截图原因是图形测试启动参数使用 Hidden，原生窗口未正常绘制。仅图形 smoke 改成 Normal，后台非图形操作仍隐藏；没有放宽像素或布局门禁。
- 持久维护目录也转交临时进程后，测试必须等待结果文件而非入口进程结束；补齐显式与自动结果文件的等待，分别检查入口成功与实际操作预期返回码（含失败场景）。
- 候选 `online-candidate-922dacc2b9ad4573addf3e4ffbcb7bc5` 的完整 `Test-InstallerLifecycle.ps1` 通过，退出码 0。覆盖图形安装/修复/卸载确认、缓存文件校验、异步卸载、静默应用卸载入口、失败传播、保留/删除设置及自定义安装目录边界。首屏/维护页/卸载确认页像素检查、空白负例与无重叠/无滚动布局检测通过。
- 本轮成功证据位于 `artifacts/validation/online-lifecycle-20260914`（标识 `87af0c9adf0a438d91d87fff0615d5f1`），运行日志 `artifacts/online-lifecycle-latest.log`。仅测试夹具被卸载/清理；真实安装没有修改。此结果仍不证明缺失运行库环境或鼠标全部目标通过。

- 新增 `scripts/New-OnlinePackage.ps1`，在唯一候选目录中发布框架依赖版 App、Guard、Settings、Setup，以及框架依赖单文件 Uninstall；加入系统 .NET Framework 编译的 Cleanup，压缩业务文件后嵌入在线引导器。旧 release 不动。
- 实际构建暴露并修复两处问题：框架依赖单文件不支持原有压缩开关；严格模式下检查 runtimeconfig 中不存在的 framework 属性会抛异常，改为检查属性集合。
- 完整脚本重新执行成功，退出码 0；候选目录 `artifacts/online-candidate-d59af0d2fff24f2eaa607153699470f1`，大小、SHA256 与构建证据路径见其中 `candidate.json`。不是正式发行，版本仍为 0.4.2，未签名。
- 上一次同源码候选 `online-candidate-a56cea300fa74350bdd273958342d1b7` 的在线 EXE 为 31,198,208 字节；依赖探测、引导器自动启动关闭、框架依赖版英文设置窗口 smoke 均退出 0。该检查不证明缺失依赖下载安装成功，也不替代真实视觉验收。
- 下一步仍需：在线内层安装/修复/卸载闭环、缺失依赖环境、首屏语言与实际界面验收、用户语言传递、版本元数据/签名状态/发布清单、其余鼠标与退出联动验收。不得将候选构建成功视为全部目标完成。

## 已实现的代码变更

### 在线安装闭环后续实测

- `Test-InstallerLifecycle.ps1` 新增可选外部包参数，仍保留全部视觉检测；在线候选实测在首屏截图检测失败（单色空白，布局指标无重叠）。增加 250 ms 首帧等待后仍失败，因此撤回无效等待改动；视觉门禁没有绕过，原因仍待定位。
- 新增独立 `Test-OnlineDeployment.ps1`，仅验证隔离目录/注册表命名空间中的 CLI 安装、缓存修复及卸载，不宣称覆盖视觉与交互。
- 首次真实缓存修复暴露维护目录自身 EXE 被占用：替换维护程序拒绝访问并导致回滚报错。修复 `RelaunchOutsideMutableRoot`：维护目录中的入口也必须先转交临时维护进程，避免自身文件占用。
- 候选 `online-candidate-b19528adcccb4ad88f7fd84bfeddcb34` 的 CLI 闭环通过（退出码 0），确实删除隔离安装内 README、缓存修复后校验原哈希、通过独立维护器卸载并检查安装目录与应用注册项移除。证据：`artifacts/online-deployment-cli-latest.log`。未改动真实已安装 FoxMouse。
- 测试临时根保留为诊断证据；卸载操作仅移除本轮创建的测试产品数据。失败轮也保留诊断目录，未递归清理用户目录。
- 修复后的全量 Release 回归再次通过：Core 100、Deployment 122、Settings 49、Platform 189，共 460/460，0 跳过；`artifacts/tests-latest.log`，进程退出码 0。

1. 倍率 NumberBox 从 132 改为 180 DIP，使用 Compact 增减按钮，为编辑区留空间。
2. 原生光标恢复不再把 WM_SETCURSOR 投递成功当作恢复成功；恢复后检查 CURSOR_SHOWING。效果结束后增加 16/32/64/128 ms 有界重试，新效果、鼠标移动或退出时停止。实际偶发消失尚未完成实机验收，不能宣称彻底修复。
3. 排除页面改为左右等宽应用列表/已屏蔽，两侧独立列表；增加已屏蔽搜索、数量和无匹配提示。搜索不改变保存的排除项。
4. 新增同用户、同会话、按配置路径隔离的 SettingsShutdownChannel。托盘退出等待设置窗口关闭，全局退出下取消也退出；单独关闭设置的取消行为不变。即使引擎退出报错也请求关闭设置。
5. 安装/升级成功进入 SucceededAwaitingConfirmation，显示确定，等待点击退出；修复/卸载保留自动退出；测试驱动负责自动确认，不影响正式交互。
6. 定位环移除白色描边，保留橙色抗锯齿描边。
7. 语言基础模型加入 language=system/zh-CN/en-US，旧配置默认跟随系统；中文区域使用中文，其他区域回退英文；不改数字区域格式。设置页面已提供语言选择，保存后重开设置生效。68 处固定文字进入 resw/x:Uid（Window.Title 例外用代码资源），动态提示进入 Core/Strings.resx 与 Strings.en-US.resx；UiText 管理语言。托盘/安装维护等仍待翻译。

## 2026-09-14 后续语言开发

- 英文启动曾因 x:Uid 给 Window.Title 赋值而 XamlParseException，真实 smoke 捕获异常；移除 Window 的 x:Uid，使用 UiText.Get("SettingsTitle") 后英文 window smoke 退出码 0。
- computer-use 实际打开 --language=en-US 窗口确认 General、Display language、Native、Medium、Maximum scale、Preview enlargement、Revert changes、Save 和辅助说明均为英文。原生开关仍显示中文，因此后续补充 8 个 OnContent/OffContent 资源项（需下一轮截图确认）。
- 语言 CLI 参数用于验收，不更改保存配置。测试窗口已关闭，没有改动用户设置。
- 原默认 940×720 物理像素改为按窗口 DPI 计算 1100×780 DIP，限制在就近显示器工作区内（需下一轮截图确认）。
- 退出监听回调改为在 UI 线程读取当前窗口，不再捕获可能已关闭的旧窗口。
- settings 的单测 49/49，core 100/100；增加英文/中文资源读取、缺失键、数字区域不变测试。
- 后续注意：App 打开设置读取语言；关闭重开生效。托盘后台仍需要设置加载/重载时 Configure。同一 UiText 默认取系统 UI 语言，测试若依赖中文文案应明确测试语言而不是假设宿主中文。

## 当前证据

- Release 解决方案曾完整编译通过，零警告零错误。
- 最新完整自动测试（语言模型和已屏蔽搜索之前）：410/410：Core 93、Deployment 80、Platform 189、Settings 48。
- 已屏蔽搜索加入后：Settings 49/49。
- 语言模型加入后：Core 99/99。随后最新完整测试 417/417（Core 99、Deployment 80、Platform 189、Settings 49），失败 0，跳过 0。
- 使用 computer-use 实际打开新构建的设置窗口，最大化后输入 4.25，数字、清除按钮、Compact 增减按钮没有重叠，失焦后保存按钮变为可用。
- 双栏已在当前桌面最大化窗口确认并排显示、加载进程正常。随后撤销测试输入，保存/撤销按钮均恢复禁用，并关闭测试窗口；没有保存到用户配置。
- 一次并行测试因验收窗口占用 DLL 导致复制失败；关闭窗口后重跑，410 项全部通过。不是掩盖的测试失败。
- 新增搜索/空提示还需再次截图确认；小窗口、多 DPI、多显示器、英文长文本未验收。

## 必须继续完成

### 2026-09-14 可执行在线引导器

- installer/BootstrapProgram.cs 已接成 Framework x64 WinExe：首屏简体中文/English、系统主题基本配色、不显示进度条；Continue 执行依赖检测/缺失下载/校验/安装后复查，然后打开框架依赖型 Setup；缺少嵌入 bundle 或业务包时在下载前失败。已有依赖全部跳过下载。执行步骤期间关闭请求延迟到步骤结束，避免强杀安装器。
- 固定 SDK 2.4.0 官方下载校验：从已确认微软官方 aka.ms 链接下载到 artifacts/online-probe-20260914/WindowsAppRuntimeInstall-x64.exe，Authenticode Valid，签发者 Microsoft Corporation；SHA512 AB866DA27FB6B1696C5399881F6F741BF963FF0D6DF6A8009BD2FE13AC604CADDD4FEB253CD86690E7CCDA61337880B3C911273F9A5324386B33AA512AB1363E。仅下载与验签，没有执行运行库安装。
- 新增 BootstrapBundle.cs：受限随机临时目录，拒绝 ZIP 路径穿越/ADS/绝对路径，限制条目数量与解压大小，FileMode.CreateNew，拒绝链接清理。增加 7 项测试；Deployment 122/122。
- 新增 scripts/New-OnlineBootstrap.ps1，从框架依赖型 Setup 发布目录构建引导器，必须同时存在 FoxMouse-package.zip；拒绝自包含 runtimeconfig/coreclr.dll、现有输出和链接源目录。生成的临时构建输入暂保留作为证据。
- Setup 支持 --language zh-CN/en-US；在线引导器通过 --package 和 --language 传入。业务包尚未生成/集成 New-Release，所以正式在线安装包未发布。
- 使用系统 csc 编译 artifacts/online-probe-20260914/FoxMouse-OnlineSetup-Developer.exe（无 bundle 的开发版）；--probe=0，--smoke-test=0。没有通过 Continue 执行安装；下一轮需要完整 bundle 与维护 UI 衔接、实际视觉验收。
- 待加强：临时目录 ACL/重解析点竞态、防资源注入与链接 ZIP 条目、重启状态单独显示、错误中文翻译、网络取消体验、旧系统 SDK/DDLM 依赖确认、应用启动健康探测。当前引导器布局和高对比/主题实时切换尚未实机验收。

### 2026-09-14 下载器与本机检测

- 新增 installer/BootstrapDownloader.cs，使用 HttpClient 禁用自动重定向并逐跳验证白名单；最多 5 次跳转、总超时 5 分钟、响应必须 200、Content-Length 和流大小校验、SHA512 校验；失败清空调用方新建的 staging stream。没有安装执行逻辑。调用方仍需实现受保护临时文件、签名复核、执行器与用户交互。
- 新增 installer/BootstrapInventory.cs：从 Program Files 与 x64 注册信息找 .NET 运行库，要求 Desktop/Core 同版本文件存在；用 GetPackagesByPackageFamily 查当前用户包并核对家族、发布者、架构和最低版本。注册存在不等于健康，最终必须启动探测。
- 新增 15 项下载/包检测测试，Deployment 115/115。基础设施通过 Windows 自带 Framework64/v4.0.30319/csc.exe 编译。
- 从该编译产物只读调用检测器：Desktop 10.0.11=true；Microsoft.WindowsAppRuntime.2 x64>=2.4.0.0=true；MicrosoftCorporationII.WinAppRuntime.Main.2 x64>=2.4.0.0=true；MicrosoftCorporationII.WinAppRuntime.Singleton x64>=8002.4.0.0=true。没有下载或安装依赖。
- 下轮优先将基础设施接成真正可执行的在线 bootstrapper（无需 .NET10），携带精简业务 payload，在首屏提供语言选择，并把语言传给维护 UI。与 New-Release 交易式打包集成，不能停留在工具类。
- 尚须补测取消、断流、无 Content-Length 超限、下载实际网络、签名锁定与执行时文件替换；检测器对受限注册表/目录应显示诊断，不应绕过。

### 2026-09-14 依赖型发布探测与校验策略

- 使用 --self-contained false -p:WindowsAppSDKSelfContained=false 成功发布设置程序到 artifacts/online-probe-20260914/settings；64 文件，82,569,093 字节。发现未使用的 ONNX 21,659,280 字节和 DirectML 18,700,224 字节由 SDK 汇总包引入。
- Settings.csproj 改为官方同一 SDK 2.4.0 组件版本：WinUI 2.3.6、InteractiveExperiences 2.1.6、Runtime 2.4.0；保留传递依赖 Foundation/Base。没有手动删除发布 DLL。
- 相同依赖型发布到 artifacts/online-probe-20260914/components：49 文件，39,865,309 字节；减少 42,703,784 字节（不是最终安装包的压缩体积）。最大剩余文件 Microsoft.Windows.SDK.NET.dll 26,341,408 字节，不盲目裁剪。
- 组件化后的正常 self-contained 构建，中文/英文 --window-smoke-test 均退出码 0。最新全套测试 438/438（Core 100、Deployment 100、Settings 49、Platform 189）。发布目录完整 UI 仍待视觉和干净环境验证。
- installer/BootstrapDependencyPolicy.cs 按 Windows 内置 .NET Framework C# 编译器兼容语法实现：严格 HTTPS 微软源白名单（重定向每一跳未来也须验证）、SHA-512 必须固定且验证成功、同 major/minor 稳定运行库最低版本、非零退出/重启要求/安装后仍缺失均不可视作安装成功。19 项新增测试通过，也使用 Framework64/v4.0.30319/csc.exe 成功编译。尚未完成实际下载/安装执行器。
- 当前计划用系统 .NET Framework 引导器（现有 Cleanup 也用此方式）避免引导器自己依赖 .NET 10；后续需核实最低支持 Windows 自带版本。
- 已从微软 release metadata 取得 Windows Desktop Runtime 10.0.11 x64：URL https://builds.dotnet.microsoft.com/dotnet/WindowsDesktop/10.0.11/windowsdesktop-runtime-10.0.11-win-x64.exe ，SHA512 4dbf26b0b78f55c5f59a46c3c81327b23a04f449f7ac6798204dcd19d99459258936daaede61d1b8c1ba523d6c26bf68bac86b3371d22e67cef235edbdc2f26c 。运行库最低要求要和最终 runtimeconfig 保持一致，不能靠猜测。
- 微软官方 SDK 下载页确认 2.4.0，x64 链接 https://aka.ms/windowsappsdk/2.4/2.4.0/windowsappruntimeinstall-x64.exe 。未下载，未取得可固定的 SHA512，未执行任何运行库安装。需要补全发布锁定清单、签名验证、包检测及离线/在线两种包构建。

### 2026-09-14 本轮新增与实现

- 用户追加：安装器第一个界面可选择中/英文。已加入 LanguageChoice（简体中文/English），默认取系统解析语言，初始页可切换，执行操作后禁用选择；切换立即更新安装、修复、卸载、提示的窗口文案。
- MaintenanceWindow 文案进入 Core 中英 resx，Deployment 新增 Core 项目引用。首屏语言切换单测验证不执行安装、英文最小窗口无重叠。ComboBox 原生高度比首选尺寸多 2 px，新增底边余量后原 4 个布局回归恢复通过。Deployment 81/81。
- 托盘菜单文字、辅助说明、状态、后台常见错误提示接入 UiText。后台初始化及外部设置重载时配置语言；菜单检测语言变化后刷新，宽度按文字实测。App 编译通过。仍需托盘英文视觉及重载集成验收。
- 尚待：Setup/Uninstall Program 自身错误框、DeploymentEngine 错误和备用 WinForms 设置/关于文字；安装语言是否传递给首次启动的应用也未实现。
- 首屏选择控件尚未用新版安装包实机截图验收；不要将单测替代视觉验收。新语言资源引入 Core 卫星程序集，发布清单需要检查 en-US/*.resources.dll 是否覆盖，精简包也必须保留。

- 完整中英资源和语言选择，设置/托盘/关于/安装/维护/卸载/错误提示一致。
- 使用原生 resw + x:Uid；附加属性资源名按 Microsoft 规范填写。参考 https://learn.microsoft.com/en-us/windows/apps/windows-app-sdk/mrtcore/localize-strings 。WinUI unpackaged 需要显式初始化语言偏好。
- 精简在线安装器尚未实现。必须在没有 .NET/Windows App SDK 的干净环境可启动，检测架构与版本，仅从官方源下载并校验依赖，网络失败可重试且不能报告成功；保留离线完整安装器。不允许简单删除运行库后称为精简包。
- 新增退出通道真实跨进程验收：无改动、保存、不保存、取消、保存失败、设置窗口重开。检查 App.xaml.cs 捕获 ownedWindow 的监听生命周期，防止窗口重建后仍引用旧实例。
- 原生光标消失真实重现与修复验证，包括静止、连续预览、切换应用、锁屏恢复、异常退出；不能以 API flags 代替肉眼像素验证。
- 定位环透明边缘截图与多 DPI 验证。
- 当前默认窗口 940×720 物理像素，需按工作区/DPI调整，保证双栏在常用缩放下可用；检查长进程标题、键盘操作及空列表。
- 安装成功确认页面的实际截图、确定/关闭按钮、升级/维修/卸载回归。
- 更新版本、CHANGELOG，构建正式产物、完整回归、生成新版验收报告和哈希。不要覆盖历史 v0.4.2 验收报告。

## 构建入口

工作区外层为 FoxDesk，项目在 FoxMouse 子目录。使用 scripts/Get-DotNet.ps1 得到 dotnet 路径；dotnet 不在 PATH。
测试/构建前先关闭自己启动的设置窗口，避免 DLL 文件锁。
