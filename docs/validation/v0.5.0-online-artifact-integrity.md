# 0.5.0 精简包产物完整性

`scripts/Test-OnlineArtifact.ps1` 是只读校验，已接入 New-OnlinePackage 的构建尾部；任意校验失败会阻止构建报告成功。

检查项：候选清单的路径、体积、SHA256；7 个程序的版本一致；主程序、WinUI 设置与安装宿主的英文资源；WinUI PRI；不夹带 coreclr、hostfxr 或 Microsoft.UI.Xaml 共享运行库；runtimeconfig 采用依赖型发布且与引导器 .NET 10.0.11 要求相容；ZIP 路径及重复项；ZIP 与应用目录逐文件大小和 SHA256 一致；应用文件无漏打包。

2026-09-14 实际运行：

```powershell
./scripts/Test-OnlineArtifact.ps1 -CandidateDirectory artifacts/online-candidate-c78e1bee24ed4fd4b17750c453efc597 -ExpectedVersion 0.5.0
```

结果 Passed；31,219,200 字节；SHA256 `40D2C3B30D56B9921AAAC40D18F68F91A1EB42D14E5A80773E4888DEA8515AEA`。

负向对照：对同一候选指定 0.0.0，明确报 Version mismatch，测试确认错误版本没有被接受。校验过程中未修改候选包。

该校验不是干净机器在线安装，也不证明 Authenticode 签名、实际显示效果或安装交互已通过。README 本轮修正了安装完成确认、在线/离线/便携区别、语言选择、候选状态和光标恢复限制；旧候选包内文档不自动更新，最终交付要重新打包。
