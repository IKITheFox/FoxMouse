# 倍率空输入修复与验证

2026-09-14。旧在线候选的独立设置进程 85488 实际清空 3.5 并按 Tab 后，输入保持空白且保存按钮启用。NumberBox 的空值为 NaN，原 ViewModel 未拦截该值。

修复：MaxScale 拒绝非有限值，保留上次有效倍率；GeneralPage 在 NumberBox 失焦后排队恢复有效显示，避免干扰用户替换文本的中间空白状态。新增 NaN、正负无穷三项 ViewModel 回归测试。

通过 computer-use 检查实际 Debug 独立英文设置窗口（进程 23352，参数 --interactive-ui-test --language=en-US）：确认编辑框为空，按 Tab 后恢复为 3.5，再次观察稳定显示；Save/Revert 保持禁用。点击标题栏关闭后查询该进程已退出，没有触发未保存提示。本例使用隔离设置，未修改正式配置。

整个 FoxMouse.slnx Debug 回归退出码 0：Core102、Deployment151、Settings64、Platform197，总计 514/514，0 跳过。此结果不替代多显示器、极端缩放及干净机器安装验收。

已重新生成在线候选 `artifacts/online-candidate-397a640861cf4da4b3d3e6a124097275/FoxMouse-OnlineSetup-x64.exe`，31,227,392 字节；SHA256 `575D1EC5698FF0CB6D71FBF0ACC3018F61404B8A96D677298ACF25CB76E85033`。静态完整性、隔离安装修复卸载及生命周期现均已通过。离线/便携已重建到 `artifacts/release/v0.5.0-candidate-55f0ee58451740a5a810482dcf664d11`，完整 Release 514/514 和整包门禁通过。未上传或覆盖历史候选。打包通过不等于新包的倍率空输入已经直接 UI 复测。
