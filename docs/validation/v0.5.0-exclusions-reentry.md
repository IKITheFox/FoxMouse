# 应用屏蔽页键盘与页面重入验证

打包更新：此次修复已纳入在线 a9caeef11ca04ccb8c5cb45d549ce186、离线/便携 81379105d2ab40cea40df1407def5439 候选，完整 Release 515/515 及整包门禁通过；下文“需打包”描述是修复发生时的历史状态。最新位置/哈希以 candidate-packages 为准。尚未完成窄窗口、干净机器依赖安装及所有直接 UI 验收。

2026-09-14，computer-use 实际操作独立英文设置配置，未操作其他应用本身。

## 发现与修复

在线候选 397a640861cf4da4b3d3e6a124097275 的进程 2752：搜索 Feishu 后，依次 Tab 聚焦 Refresh、Browse apps、Show background processes、列表项、Add，焦点轮廓可见；Return 添加成功，右栏 1/1，左栏 Added。切换到 General 再回 App exclusions，未保存屏蔽项仍在，但搜索框空白、列表仍只显示 Feishu；重复观察确认并非加载中的瞬时状态。

原因：AutoSuggestBox 的用户输入事件写入 ProcessSearch，但 Text 未绑定回 ViewModel，新页面显示默认空文本，而共享 ViewModel 仍保留筛选词。新增 Text 的 OneWay ProcessSearch 绑定，用户输入仍沿原事件更新筛选，避免程序设置 Text 触发用户输入逻辑。

关闭旧窗口，选择 Don't save；随后查询进程 2752 已退出。未保存此次屏蔽操作。

## 修复复测

设置模块 Debug 测试 64/64，0 跳过，退出码 0。实际 Debug 进程 66708 搜索 Feishu → General → App exclusions，搜索文本与单项筛选结果均保留，Save/Revert 禁用。与旧包同样步骤出现不同正确结果。该复测不证明跨进程保存重开或所有键盘删除路径。

只修改 ExclusionsPage.xaml 搜索文本绑定。现有在线 397a、离线/便携 55f0 候选均不含此次修复；需在剩余界面检查后重新打包。本轮没有修改版本号，也未上传 GitHub。

## 后续回归检查

新增 ExclusionsPageContractTests，测试项目复制实际 XAML 作为只读夹具，断言 ProcessSearchBox 保留来源绑定和用户输入处理事件。这是源码契约检查，不能替代上面的真实页面重入步骤。设置模块 65/65 通过，整个方案 Debug 515/515（Core102、Deployment151、Settings65、Platform197），0 跳过，退出码 0。首次编译的 xUnit2031 分析器错误已按 Assert.Single 谓词重载修正，后续通过结果不包含失败的首次运行。

独立进程 75676 窄窗口检查：拖动边框和系统大小菜单操作后，截图未出现可确认的尺寸变化，因此没有取得窄窗口证据，不标记通过，也不据此断言产品无法调整大小。已退出尺寸操作并关闭窗口，查询进程已退出；没有保存配置。待使用可确认的尺寸变更重新验证。
