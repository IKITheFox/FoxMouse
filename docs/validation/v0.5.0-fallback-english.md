# 备用设置英文窗体验证

2026-09-14。仅新增测试，没有修改生产程序，所以现有候选包的生产代码仍有效。

备用 SettingsForm 没有独立交互启动入口。本轮通过测试宿主直接 ShowDialog 实际生产窗体，没有人为破坏主设置程序或移动正式安装文件来触发降级。

新增两项测试：英文 Save 与 Cancel。显示实际窗体后检查两个按钮的英文、可见性及窗口内边界；修改 Enable FoxMouse 后 Save 可用，Result 返回修改值且保留 en-US。调用实际按钮后，分别返回 OK / Cancel 并关闭。没有验证配置存储或主程序应用该 Result 的链路，不称为备用完整保存落盘通过。

实际 DrawToBitmap 截图人工检查：标题、Appearance、Trigger and size、Behavior、App blocking、倍率输入与底部 Cancel/Save 文案为英文且可见。备用窗体内容区允许滚动、底部按钮固定；本图为测试宿主浅色样式，不代表生产系统深色/动态主题或高对比度验收。

截图证据：`%TEMP%/FoxMouse.FallbackLayoutTests/3e22e7961ae5435e8be7d928858f5e9f/english.png`。该图来自首次测试，首次失败原因是测试使用 Show 而非生产 ShowDialog，DialogResult 不会使非模态窗体自动关闭；纠正测试生命周期后通过。第二次通过证据目录为 `%TEMP%/FoxMouse.FallbackLayoutTests/df6a4b43fe304aa4b36741b71af274f2`。

最后 Settings 全套 **61/61** 通过（此前 59，新加 2）。本轮未重跑整个方案，不宣称已执行 510 项全套回归。

仍未验证主 WinUI 启动失败时从正式托盘自动切换备用窗口的完整路径；其他原目标未决项不变。
