# FoxMouse 0.5.1 在线安装修复候选

2026-09-15。针对用户报告的无下载进度及共享冲突（错误 32）。本轮已实施，不代表干净 Windows 全流程已通过。

## 修改

- 下载写入并 Flush 后关闭读写句柄；重新以 FileAccess.Read / FileShare.Read 打开同一个文件，再次验证 SHA512，持有只读防修改/删除锁至组件进程结束。避免原先持有写权限启动 EXE。
- 文件占用错误 32/33 最多尝试三次（间隔 250、500 ms），每次重新校验；其他异常不盲重试。取消令牌在校验前、执行前及延迟中生效。
- 下载区域增加进度条、组件名、已下载 MiB、平均 MiB/s；有 Content-Length 显示百分比，无总大小使用 Marquee。UI 刷新节流 100 ms；后到的下载进度回调在阶段结束后忽略。
- 校验/安装使用不确定进度，安装后重新检测组件，仍保留失败和重启处理。错误显示阶段、异常消息及 HRESULT，提供打开日志；日志位于 LocalAppData/FoxMouse/BootstrapLogs，不随下载暂存目录清理。
- 版本更新为 0.5.1 / 0.5.1.0。本次交付在线包；不将旧 0.5.0 离线包称为新版。

## 验证

- 完整 Debug 520/520（Core102、Deployment156、Settings65、Platform197），0 跳过。
- 真实无安装副作用的 where.exe 副本在只读校验锁持有期间启动成功，同时额外写入被拒绝。校验失败、预先取消均不执行；共享冲突重试次数有上限；下载总大小/已收字节测试通过。
- 中英、540/620 宽布局检查覆盖初始隐藏日志按钮和错误时三个按钮、进度条区域不与状态区重叠。首次测试失败是旧断言要求所有按钮启动时可见；保留初始两个按钮断言，并新增错误状态检查后通过，未跳过布局测试。
- Windows 自带 C#5 编译器首次拒绝 catch 内 await；移出 catch 后编译成功，Deployment156/156 重跑通过。
- Test-OnlineArtifact 通过：包清单、资源、版本及文件完整性。
- Test-OnlineDeployment 通过：已有依赖主机上的隔离安装、缓存修复、卸载。根标识 c8424b89eb824d5697fd39f8244cd10c。

## 交付与用户复测

在线包：artifacts/online-candidate-faba287bd85340c591494e84347a9549/FoxMouse-OnlineSetup-x64.exe。
大小：31,234,048 字节。
SHA256：A4A4598355554928A58F8CCB35E88DCCFEF0F796031ED15BBA5D626B9D59357D。

未签名、未上传。请用此包替代此前 0.5.0 在线包，在原缺依赖测试系统重新运行：确认下载有进度、下载后可进入组件安装、随后打开应用安装页面。下载阶段的实际中英/主题视觉效果和微软组件在干净机器上的执行仍待用户实测，不能由本机占用测试或布局断言替代。

如再失败，请点击“打开日志”，附日志和失败页截图；不要反复使用旧包或关闭安全软件。旧通用验收指南仍可使用，但版本/哈希以本文件为准。
